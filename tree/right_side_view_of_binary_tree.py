"""
Given the root of a binary tree, return the values
of the nodes visible from the right side, ordered
from top to bottom.

Example:
  Input:  root=[1,2,3,null,5,null,4]
  Output: [1,3,4]

Constraints:
  BFS collects the last node at each level (rightmost visible).
"""
